# 通用自动化测试框架设计方案

## 🎯 设计目标

创建一个通用的自动化测试框架，让任何 `.air` 项目都能够：
- 直接放入指定目录
- 按照命名规则自动被识别
- 无需修改内部代码即可执行
- 自动集成到测试套件中

## 📁 项目目录结构

```
your_project/
├── airtest_framework/                 # 框架核心目录
│   ├── __init__.py
│   ├── core/                         # 核心引擎
│   │   ├── __init__.py
│   │   ├── discovery.py              # 自动发现引擎
│   │   ├── executor.py               # 执行引擎
│   │   ├── reporter.py               # 报告生成器
│   │   └── config.py                 # 配置管理
│   ├── utils/                        # 工具模块
│   │   ├── __init__.py
│   │   ├── device_manager.py         # 设备管理
│   │   ├── logger.py                 # 日志管理
│   │   └── helpers.py                # 辅助函数
│   └── templates/                    # 模板文件
│       ├── test_template.py
│       └── config_template.yaml
├── tests/                            # 测试用例目录
│   ├── mobile/                       # 移动端测试
│   │   ├── login_flow.air            # 登录流程测试
│   │   ├── shopping_cart.air         # 购物车测试
│   │   └── user_profile.air          # 用户资料测试
│   ├── web/                          # Web端测试
│   │   ├── homepage_check.air        # 首页检查
│   │   └── form_validation.air       # 表单验证
│   └── api/                          # API测试
│       └── user_api.air              # 用户API测试
├── config/                           # 配置文件目录
│   ├── global_config.yaml            # 全局配置
│   ├── device_config.yaml            # 设备配置
│   └── test_config.yaml              # 测试配置
├── logs/                             # 日志目录
│   ├── execution/                    # 执行日志
│   ├── reports/                      # 测试报告
│   └── screenshots/                  # 截图文件
├── run_tests.py                      # 主执行脚本
└── requirements.txt                  # 依赖文件
```

## 🏷️ 命名规则

### .air 项目命名规则

1. **功能模块命名**: `{module}_{function}.air`
   - 例如: `login_success.air`, `cart_add_item.air`

2. **测试类型前缀**:
   - `smoke_` - 冒烟测试
   - `regression_` - 回归测试
   - `integration_` - 集成测试
   - `e2e_` - 端到端测试

3. **平台标识后缀**:
   - `_android.air` - Android专用
   - `_ios.air` - iOS专用
   - `_web.air` - Web专用
   - 无后缀 - 通用测试

### 示例命名

```
tests/
├── mobile/
│   ├── smoke_login_success_android.air
│   ├── regression_cart_checkout.air
│   ├── e2e_user_journey_ios.air
│   └── integration_payment_flow.air
├── web/
│   ├── smoke_homepage_load_web.air
│   └── regression_form_validation_web.air
```

## 🔧 核心功能设计

### 1. 自动发现机制

```python
class AirtestDiscovery:
    def discover_tests(self, test_dir="tests/"):
        \"\"\"自动发现所有 .air 测试项目\"\"\"
        
    def parse_test_metadata(self, air_path):
        \"\"\"解析测试元数据\"\"\"
        
    def categorize_tests(self, tests):
        \"\"\"按类型和平台分类测试\"\"\"
```

### 2. 执行引擎

```python
class AirtestExecutor:
    def execute_single_test(self, air_path, device_config):
        \"\"\"执行单个 .air 测试\"\"\"
        
    def execute_test_suite(self, test_list, parallel=False):
        \"\"\"执行测试套件\"\"\"
        
    def handle_test_dependencies(self, tests):
        \"\"\"处理测试依赖关系\"\"\"
```

### 3. 配置管理

```yaml
# global_config.yaml
framework:
  auto_discovery: true
  parallel_execution: false
  max_retry: 3
  timeout: 300

devices:
  android:
    - uri: "Android:///"
      name: "default_android"
  ios:
    - uri: "iOS:///"
      name: "default_ios"

reporting:
  format: ["html", "json", "junit"]
  output_dir: "./logs/reports"
  
logging:
  level: "INFO"
  file_rotation: true
  max_size: "10MB"
```

### 4. 元数据文件

每个 `.air` 项目可以包含可选的元数据文件：

```yaml
# test_metadata.yaml (可选)
test_info:
  name: "用户登录流程测试"
  description: "测试用户登录的完整流程"
  author: "测试团队"
  version: "1.0.0"
  
execution:
  timeout: 120
  retry_count: 2
  prerequisites: ["app_installed", "network_available"]
  
dependencies:
  before: []
  after: ["logout_test.air"]
  
tags:
  - "smoke"
  - "login"
  - "critical"
  
platforms:
  - "android"
  - "ios"
```

## 🚀 使用流程

### 1. 添加新测试

```bash
# 1. 将 .air 项目复制到对应目录
cp -r my_new_test.air tests/mobile/login_success.air

# 2. 框架自动发现并注册测试
# 无需任何代码修改！
```

### 2. 执行测试

```bash
# 执行所有测试
python run_tests.py

# 执行特定模块
python run_tests.py --module mobile

# 执行特定标签
python run_tests.py --tags smoke

# 执行特定平台
python run_tests.py --platform android
```

### 3. 查看结果

```bash
# 查看测试报告
open logs/reports/latest_report.html

# 查看执行日志
tail -f logs/execution/test_execution.log
```

## 🔄 自动化集成

### CI/CD 集成

```yaml
# .github/workflows/mobile_tests.yml
name: Mobile UI Tests
on: [push, pull_request]

jobs:
  mobile-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run mobile tests
        run: python run_tests.py --platform android --tags smoke
      - name: Upload test reports
        uses: actions/upload-artifact@v2
        with:
          name: test-reports
          path: logs/reports/
```

### 测试框架集成

```python
# pytest 集成
import pytest
from airtest_framework import AirtestFramework

@pytest.fixture(scope="session")
def airtest_framework():
    return AirtestFramework()

def test_mobile_smoke_tests(airtest_framework):
    results = airtest_framework.run_tests(tags=["smoke"], platform="android")
    assert all(result.success for result in results)

# unittest 集成
class MobileTestSuite(unittest.TestCase):
    def setUp(self):
        self.framework = AirtestFramework()
    
    def test_login_flow(self):
        result = self.framework.run_single_test("tests/mobile/login_success.air")
        self.assertTrue(result.success)
```

## 📊 报告和监控

### 1. 测试报告

- **HTML报告**: 可视化测试结果
- **JSON报告**: 机器可读格式
- **JUnit报告**: CI/CD集成
- **实时仪表板**: 测试执行状态

### 2. 日志管理

- **分级日志**: DEBUG, INFO, WARNING, ERROR
- **结构化日志**: JSON格式便于分析
- **日志轮转**: 自动管理日志文件大小
- **集中收集**: 支持ELK等日志系统

## 🛡️ 错误处理和恢复

### 1. 错误分类

- **环境错误**: 设备连接、网络问题
- **测试错误**: 图像识别失败、超时
- **框架错误**: 配置错误、依赖问题

### 2. 恢复策略

- **自动重试**: 可配置重试次数和间隔
- **降级执行**: 跳过失败测试继续执行
- **环境重置**: 自动恢复测试环境
- **通知机制**: 失败时发送告警

## 🔧 扩展性设计

### 1. 插件系统

```python
class AirtestPlugin:
    def before_test(self, test_info):
        \"\"\"测试前钩子\"\"\"
        
    def after_test(self, test_info, result):
        \"\"\"测试后钩子\"\"\"
        
    def on_failure(self, test_info, error):
        \"\"\"失败处理钩子\"\"\"
```

### 2. 自定义执行器

```python
class CustomExecutor(AirtestExecutor):
    def execute_single_test(self, air_path, device_config):
        # 自定义执行逻辑
        return super().execute_single_test(air_path, device_config)
```

## 📈 性能优化

### 1. 并行执行

- **设备级并行**: 多设备同时执行
- **测试级并行**: 独立测试并行运行
- **资源管理**: 智能分配设备资源

### 2. 缓存机制

- **模板缓存**: 缓存图像模板加速识别
- **设备状态缓存**: 减少设备查询次数
- **结果缓存**: 避免重复执行相同测试

## 🎯 下一步实现计划

1. **第一阶段**: 核心发现和执行引擎
2. **第二阶段**: 配置管理和报告系统
3. **第三阶段**: 插件系统和扩展功能
4. **第四阶段**: 性能优化和监控

这个设计方案将让你的 `.air` 项目真正做到"即放即用"，无需任何代码修改！