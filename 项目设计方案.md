# 🏗️ Airtest自动化测试框架 - 技术设计方案

> **文档定位**：技术架构设计文档，面向开发者和架构师  
> **使用指南**：请参考 [README.md](./README.md)

## 🎯 设计目标

### 核心理念
构建一个**零配置、自动发现、即插即用**的Airtest测试框架，让测试工程师专注于测试逻辑而非框架配置。

### 设计原则
- **约定优于配置**：通过目录结构和命名约定自动识别测试
- **最小侵入性**：无需修改现有.air项目
- **高度可扩展**：模块化设计，支持自定义扩展
- **统一管理**：集中管理多个测试项目的执行和报告

## 🏛️ 系统架构

### 架构图
```
┌─────────────────────────────────────────────────────────┐
│                    用户接口层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │  CLI接口    │  │  API接口    │  │  配置接口    │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                    核心引擎层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │ 发现引擎     │  │ 执行引擎     │  │ 报告引擎     │      │
│  │ Discovery   │  │ Executor    │  │ Reporter    │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                    工具支撑层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │ 设备管理     │  │ 文件操作     │  │ 日志系统     │      │
│  │ DeviceUtils │  │ FileUtils   │  │ LogUtils    │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────────────────────────────────────────────┘
```

### 目录结构设计

```
airtest_framework/
├── __init__.py                   # 框架入口
├── core/                         # 核心模块
│   ├── __init__.py
│   ├── discovery.py              # 自动发现引擎
│   ├── executor.py               # 执行引擎
│   ├── reporter.py               # 报告生成器
│   ├── config.py                 # 配置管理器
│   └── registry.py               # 测试注册器
├── utils/                        # 工具模块
│   ├── __init__.py
│   ├── device_utils.py           # 设备工具
│   ├── log_utils.py              # 日志工具
│   ├── file_utils.py             # 文件操作
│   └── image_utils.py            # 图像处理
├── templates/                    # 模板文件
│   ├── test_template.py          # 测试模板
│   └── metadata_template.yaml   # 元数据模板
└── config/                       # 默认配置
    └── default_config.yaml       # 默认配置文件
```

## 🔧 核心模块设计

### 1. 自动发现引擎 (Discovery Engine)

**设计思路**：基于约定的目录结构和文件命名规则，自动扫描和识别测试项目。

```python
class TestDiscovery:
    """测试发现引擎"""
    
    def __init__(self, base_path: str):
        self.base_path = base_path
        self.test_registry = TestRegistry()
    
    def discover_tests(self) -> List[TestCase]:
        """自动发现所有.air测试项目"""
        # 扫描目录结构
        # 解析命名规则
        # 加载元数据
        # 注册到测试注册表
        
    def parse_test_metadata(self, air_path: str) -> TestMetadata:
        """解析测试元数据"""
        # 从文件名解析基本信息
        # 加载可选的metadata.yaml
        # 合并配置信息
```

**命名规则解析**：
- 基础格式：`{平台}_{功能描述}.air`
- 高级格式：`{模块}_{类型}_{平台}_{功能}.air`
- 自动提取：平台、模块、测试类型、功能描述

### 2. 执行引擎 (Execution Engine)

**设计思路**：支持单个测试、批量测试、并行执行、依赖管理。

```python
class TestExecutor:
    """测试执行引擎"""
    
    def __init__(self, config: FrameworkConfig):
        self.config = config
        self.device_manager = DeviceManager()
        
    def execute_single(self, test_case: TestCase) -> TestResult:
        """执行单个测试"""
        # 设备准备
        # 环境初始化
        # 测试执行
        # 结果收集
        
    def execute_batch(self, test_cases: List[TestCase], 
                     parallel: bool = False) -> BatchResult:
        """批量执行测试"""
        # 依赖分析
        # 执行计划
        # 并行/串行执行
        # 结果汇总
```

**执行策略**：
- **串行执行**：适用于有依赖关系的测试
- **并行执行**：适用于独立测试，提高效率
- **智能调度**：根据设备资源和测试特性自动选择

### 3. 报告引擎 (Reporting Engine)

**设计思路**：多格式报告生成，支持自定义模板和扩展。

```python
class TestReporter:
    """测试报告生成器"""
    
    def generate_report(self, results: List[TestResult], 
                       format: str = "html") -> str:
        """生成测试报告"""
        # 数据聚合
        # 模板渲染
        # 文件生成
        
    def generate_summary(self, results: List[TestResult]) -> Summary:
        """生成测试摘要"""
        # 统计分析
        # 趋势分析
        # 问题识别
```

**报告格式**：
- **HTML报告**：可视化展示，包含截图和日志
- **JSON报告**：结构化数据，便于集成
- **JUnit XML**：兼容CI/CD工具

## 📋 配置管理设计

### 配置层级
1. **框架默认配置** - `airtest_framework/config/default_config.yaml`
2. **项目全局配置** - `config/global_config.yaml` (可选)
3. **测试级配置** - `test_metadata.yaml` (可选)
4. **运行时参数** - 命令行参数

### 配置合并策略
```
运行时参数 > 测试级配置 > 项目配置 > 框架默认配置
```

### 配置文件结构
```yaml
# 框架配置
framework:
  auto_discovery: true
  parallel_execution: false
  max_workers: 4
  timeout: 300
  retry_count: 1

# 设备配置
devices:
  android:
    default_uri: "Android:///"
    capabilities: {}
  ios:
    default_uri: "iOS:///"
    capabilities: {}

# 报告配置
reporting:
  formats: ["html", "json"]
  output_dir: "./logs"
  include_screenshots: true
  include_logs: true

# 日志配置
logging:
  level: "INFO"
  file_rotation: true
  max_size: "10MB"
  backup_count: 5
```

## 🔌 扩展机制设计

### 插件架构
```python
class PluginManager:
    """插件管理器"""
    
    def register_plugin(self, plugin: Plugin):
        """注册插件"""
        
    def load_plugins(self, plugin_dir: str):
        """加载插件目录"""
        
    def execute_hooks(self, hook_name: str, *args, **kwargs):
        """执行钩子函数"""
```

### 钩子点设计
- `before_discovery` - 发现测试前
- `after_discovery` - 发现测试后
- `before_execution` - 执行测试前
- `after_execution` - 执行测试后
- `before_reporting` - 生成报告前
- `after_reporting` - 生成报告后

### 自定义扩展示例
```python
class CustomPlugin(Plugin):
    def before_execution(self, test_case: TestCase):
        """执行前的自定义逻辑"""
        # 环境检查
        # 数据准备
        # 通知发送
        
    def after_execution(self, test_result: TestResult):
        """执行后的自定义逻辑"""
        # 结果处理
        # 清理工作
        # 通知发送
```

## 🚀 性能优化设计

### 并行执行策略
- **测试级并行**：多个测试同时执行
- **设备级并行**：同一测试在多设备上执行
- **混合并行**：测试和设备的组合并行

### 资源管理
- **设备池管理**：动态分配和回收设备资源
- **内存优化**：大文件流式处理，避免内存溢出
- **缓存机制**：测试发现结果缓存，提高启动速度

### 错误恢复
- **自动重试**：失败测试自动重试机制
- **故障隔离**：单个测试失败不影响其他测试
- **优雅降级**：部分功能失败时的降级策略

## 🔒 安全性设计

### 数据安全
- **敏感信息保护**：配置文件中的密码和密钥加密存储
- **日志脱敏**：自动识别和脱敏敏感信息
- **权限控制**：文件和目录的访问权限控制

### 执行安全
- **沙箱执行**：测试在隔离环境中执行
- **资源限制**：CPU、内存、磁盘使用限制
- **超时保护**：防止测试无限期运行

## 📈 监控和诊断

### 性能监控
- **执行时间统计**：测试执行时间分析
- **资源使用监控**：CPU、内存、磁盘使用情况
- **设备状态监控**：设备连接状态和性能指标

### 诊断工具
- **日志分析**：结构化日志和智能分析
- **错误诊断**：常见错误的自动诊断和建议
- **性能分析**：性能瓶颈识别和优化建议

## 🔄 版本兼容性

### 向后兼容
- **API稳定性**：核心API保持向后兼容
- **配置兼容**：旧版本配置文件自动迁移
- **测试兼容**：现有.air项目无需修改

### 升级策略
- **渐进式升级**：支持新旧版本并存
- **自动迁移**：配置和数据的自动迁移工具
- **兼容性检查**：升级前的兼容性验证

---

## 📚 开发指南

### 贡献代码
1. Fork项目仓库
2. 创建功能分支
3. 编写测试用例
4. 提交Pull Request

### 代码规范
- 遵循PEP 8编码规范
- 使用类型注解
- 编写完整的文档字符串
- 保持测试覆盖率 > 90%

### 测试策略
- **单元测试**：核心模块的单元测试
- **集成测试**：模块间的集成测试
- **端到端测试**：完整流程的端到端测试
- **性能测试**：关键路径的性能测试

---

*本文档描述了Airtest自动化测试框架的技术架构和设计理念。如需使用指南，请参考 [README.md](./README.md)。*